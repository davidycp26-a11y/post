<div class="main users-new">
  <div class="container">
    <div class="form-heading"><%= t('auth.login') %></div>
    <%= form_with(url: login_path, local: true, scope: :session, data: { turbo: false }) do |f| %>
      <div class="form users-form">
        <div class="form-body">
          <p><%= t('auth.email') %></p>
            <%= f.email_field :email, value: params.dig(:session, :email), class: "form-control" %>
          <p><%= t('auth.password') %></p>
            <%= f.password_field :password, class: "form-control" %>
          <%= f.submit t('auth.login'), class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
