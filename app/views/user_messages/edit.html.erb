<div class="main posts-new">
  <div class="container">
    <h1 class="form-heading">Edit a post</h1>
    
  <%= form_with(model: @user_message, local: true, data: { turbo: false }) do |f| %>
    <div class="form">
      <div class="form-body">
          <% @user_message.errors.full_messages.each do |msg| %>
            <div class="form-error">
              <%= msg %>
            </div>
          <% end %>
        <%= f.label :content, "Message" %>
        <%= f.text_area :content, rows: 5, class: "form-control" %>
        <%= f.submit "Save", class: "btn btn-primary" %>
        <%= link_to "Back", user_message_path(@user_message), class: "btn btn-secondary" %> 
      </div>
    </div>
  <% end %>

  </div>
</div>